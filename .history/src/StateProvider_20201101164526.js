import React, { createContext } from "react"
// data layer
export const StateContext = createContext()

export const StateProvider =(const [state, dispatch] = useReducer(reducer, initialState, init))=>{

}