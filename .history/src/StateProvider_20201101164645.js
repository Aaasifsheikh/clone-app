import React, { createContext,useContext,useR } from "react"
// data layer
export const StateContext = createContext()

export const StateProvider =(useReducer,initialState,children)=>{
  <StateContext.Provider>

  </StateContext.Provider>
}